<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzzz.dao.LecturerDao">
  <sql id="all_columns">
    lecturer_id, name, photo_url, introduction, enterprise_id
  </sql>
  <insert id="insert" parameterType="Lecturer">
    insert into lecturer(name, photo_url, introduction, enterprise_id)
    values(#{name}, #{photoUrl}, #{introduction}, #{enterpriseId})
  </insert>
  <select id="checkExistenceById" parameterType="long" resultType="boolean">
    select count(*)
    from lecturer
    where lecturer_id=#{lecturerId}
  </select>
  <select id="getById" parameterType="long" resultType="Lecturer">
    select <include refid="all_columns" />
    from lecturer
    where lecturer_id=#{lecturerId}
  </select>
  <update id="update" parameterType="Lecturer">
    update lecturer set
      name=#{name},
      photo_url=#{photoUrl},
      introduction=#{introduction},
      enterprise_id=#{enterpriseId}
  </update>
</mapper>